DATABASE_URL="postgresql://delifast:delifast_dev_password@localhost:5432/delifast"
JWT_SECRET="change-this-to-a-long-random-string"
JWT_EXPIRES_IN="7d"
ADMIN_JWT_EXPIRES_IN="12h"
PORT=4000
NODE_ENV="development"
# Comma-separated list of origins allowed to call this API.
# REQUIRED in production — e.g.:
# ALLOWED_ORIGINS="https://app.delifast.app,https://vendor.delifast.app,https://admin.delifast.app"
ALLOWED_ORIGINS=""
